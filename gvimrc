" ----------------------------------------------------------------------------------
"                                 GUI settings
" ----------------------------------------------------------------------------------
if has("gui_macvim")
    " set macvim specific stuff
	colorscheme molokai
	set guifont=Monaco:h14
	set colorcolumn=85
	set relativenumber
	set undofile
    set stal=2 " turn on tabs by default
endif
